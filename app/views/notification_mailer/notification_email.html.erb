<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
      padding: 20px;
    }
    .header {
      text-align: center;
      padding: 20px 0;
      border-bottom: 2px solid #3B82F6;
    }
    .content {
      padding: 30px 20px;
    }
    .message {
      background-color: #F3F4F6;
      padding: 20px;
      border-radius: 8px;
      margin: 20px 0;
    }
    .cta-button {
      display: inline-block;
      background-color: #3B82F6;
      color: white;
      padding: 12px 30px;
      text-decoration: none;
      border-radius: 6px;
      font-weight: 600;
      margin: 20px 0;
    }
    .footer {
      text-align: center;
      padding: 20px;
      color: #666;
      font-size: 12px;
      border-top: 1px solid #E5E7EB;
      margin-top: 30px;
    }
  </style>
</head>
<body>
  <div class="header">
    <h1 style="margin: 0; color: #1F2937;"><%= @notification.email_subject %></h1>
  </div>

  <div class="content">
    <p>Hi <%= @recipient.first_name || @recipient.name %>,</p>

    <div class="message">
      <%= @notification.message %>
    </div>

    <% if @notification.url.present? && @notification.url != "#" %>
      <div style="text-align: center;">
        <%= link_to "View Details", @notification.url, class: "cta-button", style: "color: white; text-decoration: none;" %>
      </div>
    <% end %>
  </div>

  <div class="footer">
    <p>Review Collector</p>
    <p>You're receiving this because you have notifications enabled for your account.</p>
    <p><%= link_to "Manage notification preferences", "#" %></p>
  </div>
</body>
</html>
