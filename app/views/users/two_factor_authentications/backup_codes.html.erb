<div class="max-w-2xl mx-auto p-6">
  <h1 class="text-3xl font-bold mb-6">Backup Codes</h1>

  <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
    <p class="text-yellow-800 font-semibold mb-2">⚠ Important</p>
    <p class="text-yellow-700 text-sm">Keep these codes in a safe place. Each code can only be used once.</p>
  </div>

  <div class="bg-white shadow rounded-lg p-6 mb-6">
    <div class="space-y-2 font-mono text-lg">
      <% @backup_codes.each do |code| %>
        <div class="p-2 bg-gray-50 rounded"><%= code %></div>
      <% end %>
    </div>
  </div>

  <div class="flex gap-4">
    <%= button_to "Generate New Codes",
        regenerate_backup_codes_users_two_factor_authentication_path,
        method: :post,
        data: { turbo_confirm: "Are you sure? This will invalidate all existing backup codes." },
        class: "px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700" %>

    <%= link_to "Back to Security Settings", users_two_factor_authentication_path, class: "px-4 py-2 bg-gray-200 text-gray-700 rounded hover:bg-gray-300" %>
  </div>

  <div class="mt-6 p-4 bg-blue-50 border border-blue-200 rounded">
    <p class="text-blue-800 text-sm">
      <strong>Tip:</strong> Print these codes or save them in a password manager. You'll need them if you lose access to your authenticator app.
    </p>
  </div>
</div>
