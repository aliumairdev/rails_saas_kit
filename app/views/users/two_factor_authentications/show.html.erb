<div class="max-w-2xl mx-auto p-6">
  <h1 class="text-3xl font-bold mb-6">Two-Factor Authentication</h1>

  <% if current_user.otp_required_for_login? %>
    <div class="bg-green-50 border border-green-200 rounded-lg p-4 mb-6">
      <p class="text-green-800 font-semibold">✓ Two-factor authentication is enabled</p>
      <p class="text-green-700 text-sm mt-1">Your account is protected with 2FA</p>
    </div>

    <div class="space-y-4">
      <%= link_to "View Backup Codes", backup_codes_users_two_factor_authentication_path, class: "inline-block px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700" %>

      <%= button_to "Disable Two-Factor Authentication",
          users_two_factor_authentication_path,
          method: :delete,
          data: { turbo_confirm: "Are you sure you want to disable two-factor authentication?" },
          class: "px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700" %>
    </div>
  <% else %>
    <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-6">
      <p class="text-yellow-800 font-semibold">⚠ Two-factor authentication is disabled</p>
      <p class="text-yellow-700 text-sm mt-1">Enable 2FA to add an extra layer of security to your account</p>
    </div>

    <div class="prose mb-6">
      <h2>Why enable two-factor authentication?</h2>
      <ul>
        <li>Adds an extra layer of security beyond your password</li>
        <li>Protects your account even if your password is compromised</li>
        <li>Uses industry-standard TOTP (works with Google Authenticator, Authy, etc.)</li>
      </ul>
    </div>

    <%= link_to "Enable Two-Factor Authentication", new_users_two_factor_authentication_path, class: "inline-block px-6 py-3 bg-green-600 text-white rounded-lg hover:bg-green-700 font-semibold" %>
  <% end %>
</div>
